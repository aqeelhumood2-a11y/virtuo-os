// Reserved for the per-company configuration surface (branding, installed
// Apps/Connectors management). Populated starting Phase 2. Empty on
// purpose in Phase 1A — this file exists so the import-boundary lint
// rules have a real, resolvable target.
export {};
