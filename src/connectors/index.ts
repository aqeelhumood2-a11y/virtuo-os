// Reserved for isolated external integrations (Odoo, SAP, Oracle, Square,
// Shopify, Custom API). Populated starting Phase 2 (registry/contract) and
// Phase 5 (real connectors). Empty on purpose in Phase 1A — this file
// exists so the import-boundary lint rules have a real, resolvable target.
export {};
