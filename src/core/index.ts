// Reserved for the Core platform (Auth, Users, Companies, Branches, Roles &
// Permissions, Inventory Engine, Order Engine, Audit Logs, Notifications).
// Populated starting Phase 1B. Empty on purpose in Phase 1A — this file
// exists so the import-boundary lint rules have a real, resolvable target.
export {};
