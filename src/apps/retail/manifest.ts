import type { AppManifest } from "@/app-registry/app-manifest.types";

export const retailManifest: AppManifest = {
  id: "retail",
  displayName: "Retail",
  icon: "shopping-bag",
  routeKey: "retail",
};
