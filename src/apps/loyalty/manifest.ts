import type { AppManifest } from "@/app-registry/app-manifest.types";

export const loyaltyManifest: AppManifest = {
  id: "loyalty",
  displayName: "Loyalty",
  icon: "gift",
  routeKey: "loyalty",
};
