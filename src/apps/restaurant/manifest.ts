import type { AppManifest } from "@/app-registry/app-manifest.types";

export const restaurantManifest: AppManifest = {
  id: "restaurant",
  displayName: "Restaurant",
  icon: "utensils",
  routeKey: "restaurant",
};
