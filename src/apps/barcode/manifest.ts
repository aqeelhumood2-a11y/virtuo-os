import type { AppManifest } from "@/app-registry/app-manifest.types";

export const barcodeManifest: AppManifest = {
  id: "barcode",
  displayName: "Barcode",
  icon: "barcode",
  routeKey: "barcode",
};
